
# Configuración para desarrollo local
DEBUG = True

# Direcciones IP de tu laptop
import socket
local_ip = socket.gethostbyname(socket.gethostname())

ALLOWED_HOSTS = [
    'localhost',
    '127.0.0.1',
    '0.0.0.0',
    local_ip,  # Tu IP local
    '192.168.1.*',  # Red local si necesitas
    '10.0.0.*',     # Otra red local
]

# Configuración CORS para desarrollo
CORS_ALLOWED_ORIGINS = [
    "http://localhost:8000",
    "http://127.0.0.1:8000",
    f"http://{local_ip}:8000",
    "http://localhost:3000",  # Si usas React/Vue en puerto 3000
]

# Permitir todas las IPs en desarrollo (ajustar para producción)
CORS_ALLOW_ALL_ORIGINS = True  # Solo para desarrollo!

# Headers permitidos
CORS_ALLOW_HEADERS = [
    'accept',
    'accept-encoding',
    'authorization',
    'content-type',
    'dnt',
    'origin',
    'user-agent',
    'x-csrftoken',
    'x-requested-with',
]

# Métodos permitidos
CORS_ALLOW_METHODS = [
    'DELETE',
    'GET',
    'OPTIONS',
    'PATCH',
    'POST',
    'PUT',
]
